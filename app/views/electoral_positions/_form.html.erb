<%= form_with(model: electoral_position) do |form| %>
  <% if electoral_position.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(electoral_position.errors.count, "error") %> prohibited this electoral_position from being saved:</h2>

      <ul>
        <% electoral_position.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :jurisdiction, style: "display: block" %>
    <%= form.text_field :jurisdiction %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
